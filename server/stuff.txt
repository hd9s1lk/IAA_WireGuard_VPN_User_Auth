sudo apt update
sudo apt install wireguard
sudo apt install stunnel4

//CRIAR CANAL TLS

//CRIAR CERTIFICADOS
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout  /etc/stunnel/server.key -out /etc/stunnel/server.crt

//ESCREVER Configuração DO TUNEL
ver stunnel.conf

//começar serviço
sudo systemctl enable stunnel4
sudo systemctl start stunnel4

//verificar serviço
sudo systemctl status stunnel4




//wireguard

//CHAVE PRIVADA
wg genkey | tee /etc/wireguard/server.eky


//CHAVE PUBLICA
wg pubkey | tee /etc/wireguard/server.pub

//mudar acessos da chave PRIVADA
chmod 600 /etc/wireguard/server.key

//wg0.conf (Configuração Servidor - VER FICHEIRO EM ANEXO)


//permitir acesso à porta udp 
ufw allow 51280/udp

//começar o servidor wireguard
systemctl start wg-quick@wg0

//ver estado do server
systemctl status wg-quick@wg0

//ver info do server
wg




